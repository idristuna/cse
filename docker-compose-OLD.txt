services:
    proxy:
        image: proxy
        ports:
            - 8080:8080
        restart: always
 
    web:
        depends_on:
            - proxy
        image: nginx:alpine
        restart: always